# Initialize the datafile path
datafile = "inventory.csv"

# fieldnames for datafile
# If we had to use more than one datafile, I might change the syntax a little
# e.g. instead of datafile it might be 'datafile.inventory' and then fieldnames
# might become 'fieldnames.inventory'. At that point use sqlite
fieldnames = ['name', 'price', 'quantity']
